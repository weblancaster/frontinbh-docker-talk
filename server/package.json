{
  "name": "frontinbh-server",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "express": "^4.15.4"
  },
  "scripts": {
    "prestart": "npm i",
    "start": "node index.js",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
